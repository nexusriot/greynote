{
  "name": "greynote-desktop",
  "version": "1.1.0",
  "description": "GreyNote desktop client",
  "main": "main/main.js",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "start": "npm run build && electron .",
    "test": "vitest run",
    "selftest": "npm run build && electron . --selftest"
  },
  "devDependencies": {
    "electron": "31.7.7",
    "esbuild": "^0.23.0",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "marked": "^14.1.0",
    "dompurify": "^3.1.6"
  }
}
